name: money_track
description: A new Flutter project.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.1
  equatable:
  local_auth:
  flutter_credit_card:
  image_picker:
  share:
  screenshot:
  path_provider:
  feature_discovery: 
  meta:

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true